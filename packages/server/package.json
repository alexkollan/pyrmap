{
  "name": "@pyrmap/server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "dev:mock": "tsc -b && mkdir -p ../../data && FIRMS_MOCK=1 DB_PATH=../../data/dev.db node --env-file-if-exists=../../.env dist/index.js"
  },
  "dependencies": {
    "@fastify/static": "^8.0.4",
    "@pyrmap/shared": "workspace:*",
    "better-sqlite3": "^11.7.0",
    "fastify": "^5.2.0",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node-cron": "^3.0.11",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
