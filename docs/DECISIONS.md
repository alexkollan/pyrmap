# Decision Log

Append-only. One entry = one line: `YYYY-MM-DD | scope | decision | why (≤15 words)`

2026-07-17 | shared | package.json exports needs explicit `types` condition | strict NodeNext resolution ignores top-level `types` field when `exports` present
2026-07-17 | repo | added `**/*.tsbuildinfo` to .dockerignore/.gitignore | stale buildinfo copied into build context made `tsc -b` skip emitting dist
2026-07-17 | repo | pnpm-workspace.yaml sets `injectWorkspacePackages: true` | required for `pnpm deploy --prod` to work on pnpm v10+ without --legacy
2026-07-17 | server | dev:mock runs `tsc -b && node dist/index.js`, no tsx | node's type-stripping can't resolve NodeNext .js specifiers against .ts files; tsx not on the closed whitelist
2026-07-17 | web | added `@vitejs/plugin-react` (not in literal dep list) | required to build the React+Vite combo the stack table already fixes
2026-07-18 | web | dark mode added (CARTO Dark Matter tiles), default on, deviates from plan's single-basemap §8.1 | explicit user request post-v1; localStorage-persisted, no new npm dependency
2026-07-18 | server,shared | detections table gains scan_km/track_km (migration 1); markers switched from fixed-px CircleMarker to real-meter Circle sized to satellite pixel footprint, deviating from plan's [FIXED] §8.2 marker styles | explicit user request: convey true fire extent instead of uniform dots; a wide wildfire now reads as overlapping circles
2026-07-18 | server | dayRange bumped 1->2, deviating from plan §3.2 [FIXED] "always 1" | FIRMS dayRange = UTC calendar days, not trailing 24h; 1 loses late-evening passes after midnight
2026-07-18 | server | FIRMS currently lists NO MSG/SEVIRI source (verified via data_availability) | fast geo tier dark until FIRMS restores it; resolveSources auto-recovers when it returns
2026-07-18 | web | EFFIS WMS overlays (all.hs, effis.nrt.ba.poly; layer names verified via GetCapabilities) + Open-Meteo wind, both frontend-only fetches, no npm deps | user asked for combined multi-source data; keyless free services
2026-07-18 | web | wind arrows use a rotated divIcon, exempting them from §8.2's CircleMarker rule | wind is a vector needing rotation, not a fire detection
2026-07-18 | server | geo tier fed by EUMETSAT MTG FCI fire alerts (EO:EUM:DAT:0801, CAP/XML, 10-min) via optional EUMETSAT_CONSUMER_KEY/SECRET | FIRMS dropped MSG; CAP is plain XML so zero new deps; single-entry download avoids zip
2026-07-18 | server | Sentinel-3 SLSTR FRP also available in EUMETSAT store (EO:EUM:DAT:0417), same credentials | future option; parked — NetCDF format needs an HDF5 dep
2026-07-20 | server | EUMETSAT MTG CAP alert bulletin verified to apply an undocumented significance threshold: 0 Greece hits in ~40h while VIIRS/MODIS logged 135 | motivated the LSA SAF addition below
2026-07-20 | server | added second geo source: LSA SAF MSG FRP-PIXEL "ListProduct" (HDF5, 15-min, full-disk, no threshold) via LSASAF_USERNAME/PASSWORD | live-verified 34 Greece hits in 3h vs 0 from the CAP feed in the same window; kept alongside EUMETSAT MTG (10-min, additive, not a replacement)
2026-07-20 | server | added `h5wasm` (WASM-compiled HDF5, no native build step) to parse LSA SAF's HDF5-only FRP-PIXEL product | only format offered for this product (no NetCDF/OPeNDAP option); WASM avoids native compilation on Windows dev machines
2026-07-20 | server | X API has no free tier since Feb 2026: pay-per-use, $5/1000 tweet reads; single-account timeline w/ since_id ~$15-25/mo | explicit user request; ruled out full hashtag search (different, costlier endpoint) and scraping (ToS risk)
2026-07-20 | server | added a new "reported incidents" concept, separate table/port from Detection | text reports geocoded from free text are structurally different from satellite pixels; no confirmation/decay logic applies
2026-07-20 | server,shared | new geo tier via `X_BEARER_TOKEN`: polls @pyrosvestiki (Greek Fire Service), classifies+geocodes Greek free text | explicit user request; deliberately NOT the generalized #hashtag search also proposed — parked, costlier/noisier endpoint
2026-07-20 | server | geocoding gazetteer built once from GeoNames GR.txt dump (54 regional units via ADM2 rows + genitive parsed from "Νομός X" altnames; 13341 populated places) | free, CC-BY, matches how the account casually refers to regions (old nomos names, confirmed against real posts)
2026-07-20 | server | geocoding is tiered and never guesses: settlement-precision when disambiguated by nearest-to-region, else regional-unit centroid, else the post is skipped | "SUPER reliable" reframed with the user as "never wrong, sometimes coarse or skipped" — verified against real posts including known failure modes (accusative case, accent variants, multi-clause sentences)
2026-07-20 | server | incident reports render as a distinct divIcon (purple megaphone), exempting them from §8.2's marker-style rule | same reasoning as the wind arrow exemption above — not a satellite detection at all
2026-07-20 | repo | .gitignore's `data/` line anchored to `/data/` | unanchored, it silently ignored packages/server/src/domain/data/ (the gazetteer files) too
2026-07-20 | server | extractLocationPhrase scoped to the post's first sentence only | real miss caught live: a later "του δήμου X" naming which municipality sent backup water tankers was outranking the actual fire location in the first sentence
2026-07-20 | server | each skipped incident post now logs individually (id, extracted settlement/region if any, truncated text), not just an aggregate count | posts are human-written, not templated output — the Paleochori miss was only found because the user happened to notice; future misses need to surface in logs, not require a user report
2026-07-20 | server | poll-incidents cadence raised from 15min to every 1min | explicit user request; since_id makes an empty poll (the common case) free under X's per-tweet-read billing — user to confirm actual $ via their own billing dashboard, not independently verified by the agent
2026-07-20 | web | incident markers redesigned from an emoji-in-circle to an SVG map-pin (flame cut into a white disc, anchored at the pin tip) | emoji glyph rendering is OS/browser-inconsistent; a pin shape also reads more correctly as "a human pointed here" vs the satellite markers' centered circles ("detected here")
2026-07-20 | server | superseded line 28's "first sentence only" scoping with explicit "assistance from X" keyword exclusion around του-δήμου matches | live-caught: Greek abbreviation periods (Ε.Ι.Χ., Ν. for Νέα) appear before the real sentence end and broke the sentence cut, silently dropping a real post
2026-07-20 | server | PHRASE regex no longer allows bare "." inside a captured place name; short Greek abbreviations (Ν., Αγ., Παλ.) matched via an explicit prefix instead | non-greedy phrase capture was terminating at an abbreviation's own period (e.g. captured "Ν" instead of "Ν. Σμύρνης") rather than the real clause boundary
2026-07-20 | server,web | added /api/events (SSE) + UpdateBus: scheduler jobs push a bare "refresh" signal on actual data changes, browser refetches /api/fires immediately | explicit user request for live updates without manual refresh; no new dependency (native EventSource + Fastify reply.hijack()); 5-min poll kept as fallback
2026-07-20 | web | marker color switched from fixed per-tier (red/orange) to a 5-stop age gradient (red->orange->green->blue->grey), scaled 24h for satellites / 12h for reported fires, deviating from plan's [FIXED] §8.2 palette | explicit user request; confirmed/unconfirmed trust preserved via border color (red ring) and dash pattern instead, kept orthogonal to the new color channel
2026-07-20 | repo | packages/server/public/ (Docker's build-output copy target) added to .gitignore | manually copying the web build there for local browser verification made ESLint lint the minified bundle as source and fail
2026-07-20 | server | settlement disambiguation switched from nearest-to-region-reference-point to highest-population-within-plausible-range | live miss: "Πέραμα" picked an unpopulated (pop. 0) GeoNames dot over the real 25k-person port town near Piraeus, because the empty one was slightly nearer Attica's crude single-point region reference — that reference is not a true centroid and shouldn't be trusted for fine disambiguation
2026-07-20 | server | settlementCandidates now expands "Ν." -> "Νέα" and tries a trailing "-ς" strip (genitive) alongside the existing "+ς" (accusative) fallback | live miss: "Ν. Σμύρνης" (Nea Smyrni, abbreviated+genitive) matched neither and fell to the coarse regional_unit tier despite Nea Smyrni (pop. 73,076) being in the gazetteer under "Νέα Σμύρνη"
2026-07-20 | repo | Dockerfile healthcheck uses `node -e ...http.get...` instead of `wget` | node:22-slim has no wget/curl at all — the old healthcheck failed unconditionally, marking a genuinely-healthy container "unhealthy" in Portainer; verified: built the runtime image, ran a real container, confirmed Docker reports "healthy" (exit 0) with the fix, "unhealthy" (wget: not found) without it
2026-07-20 | web | added an SVG favicon (flame, matches the app's red/orange gradient + dark theme) | none existed before; verified rendered legibility at 16-64px via a live screenshot before committing
2026-07-20 | server | single-user auth is hand-rolled (HMAC-SHA256 signed cookie via node:crypto), not a new dependency (e.g. @fastify/session) | explicit user request for exactly one hardcoded username/password with persistent sessions — too small a need to justify a new dependency; no password hashing library either, since there's one credential pair from .env, not a user table
2026-07-20 | server | auth is opt-in via AUTH_USERNAME+AUTH_PASSWORD+SESSION_SECRET (all three or none) | keeps local dev frictionless (open by default) while making a half-configured .env impossible to mistake for "protected" — for a live public deployment, this needs to be enabled by the operator explicitly, not enabled by accident
2026-07-20 | server | static frontend and /api/health stay reachable without login; only /api/fires, /api/status, /api/events require a session | the SPA shell needs to load to render its own login form, and Docker's own healthcheck must never require a browser session
2026-07-20 | server | extractLocationPhrase: added "στο/στον δήμο X" (accusative) alongside "του δήμου X" (genitive), and the phrase now also ends at end-of-string, not only at a period/comma | live miss caught during auth verification: a short post with no trailing punctuation and the accusative phrasing was skipped entirely
2026-07-20 | repo | docker-compose.yml's data volume switched from relative `./data` to `${HOST_DATA_DIR:-./data}` (absolute path for Portainer) | live-verified risk: for a Git-deployed stack, a relative bind mount resolves inside Portainer's own ephemeral git-clone working directory, which is removed on stack deletion — would have silently destroyed the database on the very next stack recreation; defaults to the old relative behavior for plain local `docker compose up`
2026-07-20 | repo | docker-compose.yml's `environment:` block was missing AUTH_USERNAME/AUTH_PASSWORD/SESSION_SECRET entirely | root cause of prod auth never activating despite correct Portainer config: compose never forwarded those three vars into the container regardless of what Portainer set, unrelated to the earlier git-auth saga; live-verified via `/api/me` returning 404 (open) before the fix
2026-07-22 | server | settlementCandidates gains a "-ών" -> "-ές" declension transform | live miss: "Αχαρνών" (genitive plural of Αχαρνές/Menidi) matched neither existing transform (+ς, trailing -ς strip), zero candidates despite being in the gazetteer
2026-07-22 | server | no-region settlement ambiguity now resolves when one candidate's population exceeds all others combined (pickIfDominant), else still returns null | live miss: "Νάουσας" with no region (3 namesakes, top dwarfs the rest 19887 vs 3134+0) was wrongly refused; kept null for genuinely-ambiguous cases like "Άγιος Γεώργιος" (61 namesakes, no dominant one) per existing test
2026-07-22 | server | confirmed both misses were from @pyrosvestiki (the account already polled), not a hashtag-search coverage gap; kept #πυρκαγια search parked per 2026-07-20 decision -- costlier/noisier endpoint, doesn't fix a geocoding bug
2026-07-22 | server | settlementCandidates rewritten to decline each word of a multi-word name independently (cartesian product), not just the phrase's trailing suffix | needed to resolve genitive compound names like "Αγίου Δημητρίου" (both words declined) -- verified behavior-identical to the old single-suffix approach for all previously-passing single-word/tail-only cases
2026-07-22 | server | added "-ου"->"-ος" (masculine genitive), "-ου"->"-ο" (neuter genitive), and "-ίου"->"-ι" (the very common "-ι"-stem class: Κορωπίου->Κορωπί, also Περιστέρι/Χαϊδάρι/Μαρούσι) declension transforms | live-tested gaps: "Ωρωπού", "Λαυρίου", "Κορωπίου" all resolved to at best regional_unit precision or null despite being in the gazetteer
2026-07-22 | server | geocodeGreekLocation retries the whole phrase as one settlement name when the split's "region" word isn't a real region, but does NOT fall back further to the bare first word alone if that also fails | live-tested trap: "Άγιος" alone is a real unrelated place (pop. 801) that would otherwise wrongly win for any unresolved "Άγιος X" compound
2026-07-22 | server | known, unfixed limitation: extractLocationPhrase returns only ONE location per post; a post naming two separate fires would silently drop the second | no live example has confirmed this happens yet; flagged so a future miss is recognized as this known gap rather than re-investigated as new -- fixing it needs a schema/dedup-key change (one row per post today), out of scope for this round
2026-07-22 | server | added `web-push` (+ @types/web-push) | explicit user request for push notifications; outside the closed dependency whitelist (plan §15), justified at the import site
2026-07-22 | server | push notification auth gating: /api/push/subscribe + /api/push/unsubscribe require a session when auth is configured (same protected group as /api/fires); /api/push/vapid-public-key stays open like /api/health | explicit user decision during brainstorming; public keys aren't sensitive, and the frontend needs it before any login state exists
2026-07-22 | server | one push notification per individual new detection/incident row, both FIRMS tiers always, no re-notify on confirmation upgrade | explicit user decision during brainstorming, overriding the batched-by-default recommendation; single user, wants maximum immediacy over noise reduction
2026-07-22 | web | PWA implemented hand-rolled (manifest + sw.js), not via vite-plugin-pwa | app is live-data-dependent, not offline-first; avoids a new frontend build dependency entirely
2026-07-22 | web | PNG icons generated once via `npx sharp-cli` from the existing favicon.svg, not committed as an ongoing dependency | one-time asset conversion; no rasterization tool was available locally (checked rsvg-convert/imagemagick/inkscape, none present)
